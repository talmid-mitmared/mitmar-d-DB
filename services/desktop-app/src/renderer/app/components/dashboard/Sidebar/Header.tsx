import { SidebarHeader } from '../../../shared/ui/Sidebar';
import { Switcher } from './Switcher';

export function Header() {
  return (
    <SidebarHeader>
      <Switcher />
    </SidebarHeader>
  );
}
