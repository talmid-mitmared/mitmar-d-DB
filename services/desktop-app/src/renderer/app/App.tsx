import { Header } from './components/Header';

const App = () => {
  return (
    <div className="h-screen flex flex-col overflow-hidden w-screen">
      <Header />
    </div>
  );
};

export default App;
